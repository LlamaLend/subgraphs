{
  "name": "llamalend",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ llamalend",
    "create-local": "graph create --node http://localhost:8020/ llamalend",
    "remove-local": "graph remove --node http://localhost:8020/ llamalend",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 llamalend"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.0",
    "@graphprotocol/graph-ts": "0.22.0"
  }
}
